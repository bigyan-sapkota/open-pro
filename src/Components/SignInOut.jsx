import React from "react";
import Button from "./Button";

const SignInOut = () => {
  return (
    <div>
      <Button text="Sign In" bgColor="#4b4acf" />
      <Button text="Sign Out" bgColor="#fff" />
    </div>
  );
};

export default SignInOut;
